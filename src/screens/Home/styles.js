import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
    container: {
        flex: 1,
        padding: 20
    },
    title: { fontSize: 50, color: '#4f83cc', fontWeight: 'bold' },
})